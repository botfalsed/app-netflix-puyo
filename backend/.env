# Database Configuration
# Option 1: Use DATABASE_URL (PostgreSQL connection string)
DATABASE_URL=postgresql://postgres:mrmarco64@localhost:5432/netflixdb

# Option 2: Individual database settings (fallback if DATABASE_URL fails)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=netflixdb
DB_USER=postgres
DB_PASSWORD=mrmarco64

# Server Configuration
JWT_SECRET=please_change_this_to_a_strong_secret
PORT=4000
NODE_ENV=development
