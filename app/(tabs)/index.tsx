import React from 'react';
import NetflixScreen from '../../components/NetflixHome'; // ⬅️ Asegúrate que la ruta sea correcta

const App = () => {
  return (
    // Renderiza el componente que contiene toda la lógica de la pantalla de inicio de sesión
    <NetflixScreen />
  );
};

export default App;